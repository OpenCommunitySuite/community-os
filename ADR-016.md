# ADR-016 — Reliable background and integration runtime

**Статус:** Proposed / Предложено
**Язык документа:** русский

---

## Контекст

ADR-011 различил domain semantics, integration semantics, external protocol и transport, а также redelivery, duplicate, correction, replacement, new information, failure и Unknown Outcome. ADR-012 определил persistent/resumable provisioning, Community lifecycle, Placement и Placement Generation. ADR-013 принял отдельный Worker Host, Durable Application Events, durable delivery и transactional outbox или equivalent для надёжного продолжения после local commit. ADR-014 ограничил ACID одной resolved Transactional Persistence Boundary и потребовал recoverable cross-boundary processes. ADR-015 определил Workload Identity, current-sensitive revalidation, Secret References и Security Audit.

Нормальные runtime failures происходят между этими границами: Worker завершается, acknowledgement теряется, delivery повторяется, external provider отвечает с задержкой или не позволяет установить результат, Community меняет lifecycle/Placement, authorization отзывается, а queued work переживает deployment. Такая работа не может полагаться на process memory, exactly-once предположение, открытую local transaction во время внешнего вызова или captured user credential.

## Проблема

Требуется единая минимальная архитектурная семантика для фоновой, отложенной, scheduled и integration work, которая:

- переживает crash/restart/deployment там, где obligation является значимым;
- допускает repeated delivery/execution без непреднамеренного повторения effect;
- различает Known Success, Known Failure и Unknown Outcome;
- сохраняет identity, causation, history и безопасную диагностику;
- повторно проверяет current-sensitive gates;
- соблюдает Community/Placement isolation;
- поддерживает resumable multi-step processes;
- не присваивает общему runtime domain meaning конкретных modules/contexts.

## Область решения

ADR определяет Persistent Work contract, Operation и Attempt identity, durable delivery/outbox, selective duplicate recognition, at-least-once exposure, idempotency boundary, failure/quarantine, Unknown Outcome/reconciliation, execution revalidation, scoped ordering/exclusivity/concurrency, fairness/backpressure, cancellation/compensation, Process Coordinators, Placement safety, security/observability и work-contract compatibility.

В качестве validation scope рассматриваются provisioning/relocation, imports, telemetry, bank synchronization, notifications, DNS/ACME, object operations и другие external interactions. Их domain/integration policy остаётся у owning context/contract.

## Не является целью

ADR не выбирает broker, queue topology, framework/library/provider, scheduler, job store, SQL schema, serialization, lease/lock algorithm, poll interval, retry/backoff formula, timeout, worker count, exact quota, deployment topology, HA/monitoring product или runbook.

ADR не вводит universal domain Job, universal Workflow Engine/State Machine, universal Event/Message entity, universal Inbox, global/per-Community FIFO, distributed transaction или system-wide exactly-once guarantee.

## Терминология и ключевые различия

**Persistent Work** — durable technical work, obligation и state которой должны переживать потерю process. Один **Persistent Work Item** представляет одну stable **Persistent Work Operation**. В common runtime contract stable Persistent Work identity является stable Operation identity.

**Attempt** — отдельный execution episode одной Operation. Retry или допустимый Resume сохраняет Operation identity, но создаёт новую Attempt identity.

**Durable Delivery Record** представляет delivery/publication obligation. Он имеет отдельную delivery identity и не является автоматически Persistent Work Operation, semantic Event или domain fact.

Не смешиваются:

```text
Persistent Work Operation
≠ Attempt
≠ Durable Delivery Record
≠ semantic Domain/Application Event or Fact
≠ external request / external response
≠ received external information
≠ recognized domain fact
≠ Security Audit
≠ Operational Log
```

Также различаются:

```text
Known Success ≠ Known Failure ≠ Unknown Outcome
Retry ≠ New Intent
Redelivery ≠ Duplicate ≠ Correction ≠ Replacement ≠ New Information
Cancellation Requested ≠ Execution Stopped ≠ Compensation Performed
Quarantined ≠ Cancelled
Cancelled ≠ Compensated
Superseded ≠ silently deleted
```

Эти понятия образуют технический runtime contract, а не новый bounded context или universal domain model.

## Решение

### 1. Persistent Work contract

Business-significant asynchronous obligation, которая должна пережить process termination/restart/deployment, фиксируется как Persistent Work или equivalent durable delivery obligation.

Минимальный общий technical contract позволяет определить:

- stable Operation identity;
- owning module, work type и contract version;
- Platform или trusted Community Scope;
- eligibility/scheduling и execution/outcome condition;
- Attempts;
- correlation и causation;
- failure/outcome classification;
- необходимые безопасные runtime references.

Точный состав physical record не определяется. Specialized payload/state/checkpoints, domain meaning/effects, recognition, correction и compensation принадлежат owning module/context. Общий runtime не получает ownership domain facts и не становится universal Workflow.

Persistent Work obligation не считается исполненной только потому, что work была claimed, external request отправлен или transport acknowledgement получен. Completion evidence определяется owning contract.

### 2. Persistent Work Operation и Attempt

Operation identity стабильна на протяжении automatic retries и допустимых resumes. Каждая execution attempt имеет собственную identity и сохраняет outcome/failure evidence, достаточные для диагностики без secret material.

Resume сохраняет Operation identity только при одновременном сохранении:

- того же unresolved obligation;
- того же semantic intent;
- того же idempotency boundary.

Materially changed intent/significant input, Correction, Replacement или deliberate new Replay создают новую linked Operation identity. Они не маскируются под Retry старой Operation. Prior Attempts и исходная Operation не переписываются.

Claim/lease может ограничивать concurrent execution, но сам по себе не доказывает exactly-once effect: Worker может завершиться после commit и до acknowledgement.

### 3. Durable Delivery и Transactional Outbox

Когда local state transition и required durable publication/continuation intent не должны расходиться, intent фиксируется atomically с local state в той же local ACID transaction посредством Transactional Outbox или semantic equivalent.

Dispatch выполняется после commit и может повторяться. Rollback local transaction не создаёт authoritative publication obligation. Delivery acknowledgement, retry и deduplication не меняют semantic identity опубликованного Event/fact.

Platform↔Community, cross-Placement и external interactions используют stable references и durable continuation; они не предполагают cross-boundary FK, SQL join, distributed ACID или 2PC.

### 4. Duplicate recognition / Inbox

Inbox не является universal mandatory component. Durable duplicate recognition обязательна там, где repeated inbound/durable delivery иначе способна создать unintended second effect и это нельзя безопасно установить из authoritative local state.

Допустимы Inbox, local uniqueness по qualified Operation/external identity, guarded state transition или другой semantically equivalent mechanism. Equal payload/hash сам по себе не доказывает duplicate.

Owning integration contract различает redelivery, duplicate, correction, replacement и new information. Повторно полученная external information не становится автоматически новым recognized domain fact и не теряется как duplicate без contract evidence.

### 5. At-least-once и idempotency

Durable delivery и Persistent Work предоставляют **at-least-once delivery/execution exposure**. После crash, timeout или lost acknowledgement та же Delivery или Operation может быть dispatched, claimed или attempted повторно.

Community OS не обещает system-wide:

- exactly-once execution;
- exactly-once delivery;
- exactly-once external effect.

Specific bounded effect может иметь effectively/exactly-once committed result только если его concrete local transactional или provider contract действительно обеспечивает это. Такая гарантия не становится свойством общего runtime.

Если повтор может создать unintended effect, owning contract определяет idempotency boundary и использует применимую комбинацию stable Operation identity, local uniqueness, guarded transition, provider idempotency, durable duplicate recognition или reconciliation. Idempotency key является qualified contract value, а не universal identity или payload hash.

### 6. Failure / outcome semantics

Архитектурно различаются, без требования одного universal enum/state machine:

- pending/eligible obligation;
- claimed/running Attempt;
- completed/succeeded Operation согласно owner-defined evidence;
- known transient failure;
- known permanent validation failure/rejection;
- authorization/admissibility/lifecycle denial;
- stale Placement Generation или incompatible contract/schema/application state;
- revoked credential/Secret;
- provider rate limit/unavailability;
- duplicate/redelivery;
- poison/malformed work;
- partial completion;
- Unknown Outcome;
- cancellation requested и execution stopped;
- superseded work;
- terminal/quarantined condition и manual intervention requirement.

Known transient failure может retry-иться согласно context policy и после revalidation. Permanent rejection, mandatory denial, incompatible work или poison input не retry-ятся бесконечно. Superseded work сохраняется traceably, а не удаляется молча.

Exact status enum, retry count, delay/backoff, circuit breaker, timeout и exception mapping принадлежат Implementation Baseline/context policy.

### 7. Terminal / quarantine

Work, которую нельзя безопасно автоматически продолжать, сохраняется в explicit terminal/quarantined condition:

> automatic processing stopped; explicit resolution is required.

Сохраняются stable Operation identity, Attempt history, classification/reason, correlation/causation, safe diagnostics и допустимый resolution path. Quarantine не означает deletion или обязательную forever-impossibility.

Manual Resume продолжает ту же Operation только внутри того же obligation/intent/idempotency boundary. Replay/Correction/Replacement/New Intent создаёт linked new Operation. Quarantined, Cancelled/Stopped и Compensated являются разными semantics и могут требовать разных resolution actions.

### 8. Unknown Outcome и reconciliation

Unknown Outcome означает, что external effect мог произойти, но доступных evidence недостаточно для Known Success или Known Failure. Он не является failure, success и не превращается автоматически в permanent failure после N retries.

Когда contract поддерживает, resolution следует использовать в порядке применимости:

```text
stable provider idempotency / correlation
→ provider status or query
→ later independent evidence and reconciliation
→ attributable manual resolution
```

Если provider не позволяет доказать outcome, а повтор способен создать duplicate material effect, blind retry запрещён. Operation остаётся unresolved/quarantined до безопасного resolution.

Bank mutation, notification provider, DNS/API mutation и object upload определяют собственные success evidence и reconciliation semantics. Notification send/provider acceptance не доказывает delivery/receipt/read/legal notification; object existence не доказывает recognized domain completion.

### 9. Causation, authority и execution revalidation

Operation может сохранять historically fixed acceptance/causation context:

- initiating Technical Identity/User Account;
- actual/represented Subject и basis только где они действительно применимы;
- original command/request и Operation identity;
- факт допустимого acceptance;
- rule/configuration/mapping/integration-contract versions, необходимые для historical determinability.

Captured acceptance/authorization является evidence/causation и не является credential, reusable delegation или evergreen authorization token. Captured human credentials не сохраняются и не используются.

Worker исполняет под собственной Workload Identity через те же Application/Domain boundaries, что synchronous entry. Перед execution или irreversible effect каждая Attempt re-resolves/revalidates declared current-sensitive gates, где применимо:

- Community lifecycle и Entitlement execution gate;
- current technical Access Rights;
- current domain Power/Representation/admissibility;
- current authoritative domain state;
- Placement и Placement Generation;
- integration enabled/configuration state;
- authorized Secret Reference/credential lifecycle;
- work-contract/schema/application compatibility.

Owning operation contract явно определяет authorization boundary. Already-final accepted durable command может технически продолжаться, если именно acceptance окончательно зафиксировал authority/intent. Delayed effect, для которого authority должна существовать непосредственно при effect, revalidates current authority.

Same Operation identity при Retry/Resume не bypass revalidation. Mandatory domain denial не может быть overridden Worker, retry, replay, support elevation, cancellation processing или другим technical runtime. Fake Subject/System User не создаётся.

### 10. Ordering, exclusivity и concurrency

Ordering, Exclusivity/Single-flight и Concurrency Limit являются разными guarantees:

- Ordering ограничивает относительный порядок processing/effects в declared stream;
- Exclusivity запрещает overlapping execution для qualified key;
- Concurrency Limit ограничивает parallelism, но сам по себе не гарантирует order или mutual exclusion.

Global FIFO и implicit per-Community FIFO отсутствуют. Owning context/process/integration contract объявляет qualified key и требуемую guarantee только там, где этого требует invariant или external side-effect safety.

Без declared guarantee handlers допускают parallelism, retries/redelivery и out-of-order delivery там, где contract это разрешает. Optimistic concurrency остаётся default; targeted serialization допустима. Database locks не удерживаются через remote call или human interaction.

### 11. Fairness и backpressure

Runtime поддерживает architecture capability для:

- Community-scoped fairness;
- workload-class isolation;
- provider/integration rate limiting;
- scoped concurrency limiting;
- backpressure;
- failure/poison containment.

Fairness может задерживать ordered work, но не нарушает declared ordering invariant. Poison work не блокирует indefinitely unrelated keys/Communities; policy внутри того же strict ordered stream принадлежит owning context. Ограничение одного provider не должно глобально блокировать unrelated providers.

Это требование не означает queue/Worker/database/broker per Community, strict equal throughput, guaranteed latency или конкретный scheduler/algorithm/quota/weight.

### 12. Cancellation и compensation

Cancellation Request выражает intent остановить future execution на explicit safe boundary:

```text
Cancellation Requested
≠ Execution Actually Stopped
≠ Compensation Performed
```

До effect boundary cancellation может предотвратить effect. После local/external irreversible effect она только прекращает допустимые future steps; уже произошедшие effects и history не стираются. При race фактический result устанавливается из authoritative state/evidence, а не из самого request.

Compensation или Correction является separate explicit, historically traceable action, обычно linked Operation со своей execution/history. Возможность и meaning компенсации принадлежат owning module/process. Universal rollback не обещается.

Cancellation sensitive work и запуск compensation должны быть authorized; утрата rights исходным initiator не препятствует independently authorized lifecycle/security stop, но не даёт инициатору evergreen authority.

### 13. Specialized Process Coordinators

Provisioning, relocation, imports, bank synchronization, bulk notification, document publication plus delivery, telemetry processing, certificate/DNS automation, export/deletion/recovery и DB/object convergence используют specialized Process Coordinator, когда имеют significant multi-step state.

Coordinator сохраняет stable process/Operation identity, owner-specific checkpoints, step idempotency, partial-success state, resumability, forward recovery, cancellation boundary и compensation/manual-resolution path. Он не присваивает domain facts participating modules и не создаёт universal Workflow Engine.

### 14. Community Scope, Placement и relocation

Каждая Community-scoped Operation имеет trusted Community Scope, полученный через разрешённый application/control-plane context, а не доверенный произвольный payload `community_id`.

Attempt разрешает current Placement и проверяет Placement Generation в применимой transaction/write boundary. Captured connection/location не является authority. Stale Generation не пишет в old Placement после cutover и приводит к rejection/re-resolution.

Work queued до relocation может продолжиться в current authoritative Placement как та же Operation с новой Attempt, если её obligation/gates остаются действительными. External effect, отправленный до relocation, не повторяется только из-за relocation; его identity/outcome reconciliation сохраняются.

Committed delivery/work obligations не теряются при relocation. Exact drain/copy/forward/fencing/lease implementation deferred. Platform-global work классифицируется явно и не получает tenant visibility по отсутствию Community scope. Dedicated Placement использует ту же logical runtime model.

### 15. Security и Secrets

Workload Identity имеет least privilege, explicit purpose и Platform/Community Scope. Privileged maintenance identities отделены от normal Worker runtime.

Persistent Work payload, Attempt error, operational log, Security Audit и domain history не содержат plaintext password, OTP, session/API token, Secret material или cryptographic key material. Work сохраняет opaque Secret Reference и безопасную metadata только где необходимо. Execution получает material через current authorized Secret lifecycle; revoked credential не восстанавливается из queued payload.

Manual Resume/Replay, quarantine release, reconciliation override, sensitive cancellation и compensation trigger выполняются авторизованно и attributable. Security-sensitive/privileged cases записываются в Security Audit с operator identity, scope, reason и result, отдельно от domain history/integration provenance/operational log.

Support elevation и break-glass не продолжаются автоматически из-за queued Operation и не превращаются в captured credential или impersonation.

### 16. History, audit, provenance и observability

Различаются:

1. Persistent Work/Process state — authoritative technical state obligation, eligibility, attempts, progress и resolution;
2. context-owned Integration Provenance — external identity/request/response/evidence/mapping/recognition;
3. Domain History — semantic facts, versions, corrections и attribution;
4. Security Audit — security-sensitive technical actions;
5. Operational logs, metrics, traces и alerts — diagnosis/health.

Для operator diagnosis доступны stable Operation/Attempt identities, owner/type/version, scope, eligibility, failure/outcome classification, safe diagnostics, correlation/causation, terminal/cancellation state и resolution linkage. Raw logs не являются durable work record, business fact или единственным evidence unresolved obligation.

Community administrators/users получают только purpose/scoped views по owning contracts, а не raw infrastructure log или unrestricted Security Audit.

### 17. Contract/version compatibility

Persistent Work contract version определима. Durable work, пережившая deployment, исполняется только supported compatible handler/schema/application state.

Unsupported work type/version fail closed: work defer-ится или quarantine-ится без silent reinterpretation, deletion или loss obligation. Historically required rule/configuration/mapping/integration-contract versions сохраняются и не заменяются молча текущими.

Placement Generation, Schema Version, work-contract version, integration contract/mapping version и domain Rule Version различаются. Serializer/upcaster/migration mechanism и rolling deployment topology не определяются.

## Validation scenarios

1. **Worker crash до обработки.** Durable obligation остаётся eligible; domain effect не inferred.
2. **Crash после local commit до acknowledgement.** Delivery/Attempt повторяется; idempotency/guarded state обнаруживает committed effect.
3. **External timeout после возможной обработки.** Outcome остаётся Unknown; blind retry запрещён, применяется reconciliation.
4. **Одинаковый webhook повторён или исправлен.** Qualified external semantics отличают redelivery от correction/new information; payload equality недостаточно.
5. **Secret/Right/Power/Entitlement отозваны после enqueue.** Attempt применяет operation-specific current gates; captured acceptance не credential.
6. **Community suspended после enqueue.** Normal background effect прекращается; explicitly authorized recovery/read/export path может продолжиться без rewrite domain facts.
7. **Community relocated.** Retry сохраняет Operation, создаёт Attempt, resolves current Placement/Generation; stale side не получает write.
8. **Poison work в ordered stream.** Work quarantine-ится; unrelated keys/Communities продолжают работу; same-stream policy сохраняет invariant.
9. **Cancellation races with external success.** Request не доказывает stopped/rollback; evidence определяет outcome, future steps/compensation обрабатываются отдельно.
10. **Old queued contract после deployment.** Supported version интерпретируется; unsupported fail closed/quarantine без silent loss.
11. **Manual Resume после quarantine и relocation.** Same Operation только при том же obligation/idempotency boundary; новая Attempt revalidates gates/location.
12. **Provider idempotency key больше не гарантирован.** Runtime не предполагает safe retry; status/reconciliation/manual resolution определяют дальнейшее действие.

## Архитектурные инварианты

1. Significant asynchronous obligation, которая должна пережить process loss, durable.
2. Один Persistent Work Item представляет одну stable Operation; Attempt и Delivery Record имеют отдельные identities.
3. Retry/Resume сохраняет Operation только внутри того же intent/obligation/idempotency boundary.
4. Correction, Replacement, New Intent и materially new Replay создают linked new Operation.
5. Worker использует те же Application/Domain boundaries и исполняет под Workload Identity, не Account/Subject.
6. Одна ACID transaction ограничена одной resolved Transactional Persistence Boundary.
7. Commit-coupled publication intent atomic с local state через outbox/equivalent; dispatch may repeat.
8. At-least-once delivery/execution exposure является baseline; universal exactly-once отсутствует.
9. Idempotency и duplicate recognition определены qualified effect/contract; universal Inbox отсутствует.
10. Redelivery, duplicate, correction, replacement и new information distinct.
11. External/received information и recognized domain fact distinct.
12. Known Success, Known Failure и Unknown Outcome distinct; Unknown не становится failure по retry count.
13. Blind retry Unknown Outcome, способный duplicate material effect, запрещён.
14. External effect не выполняется внутри open local domain transaction.
15. Persistent Work state, Domain History, Integration Provenance, Security Audit и Operational Log distinct.
16. Historically significant rule/configuration/mapping/contract version determinable.
17. Каждая Attempt применяет declared current-sensitive revalidation.
18. Mandatory domain denial не может быть overridden.
19. Lifecycle/Entitlement denial не уничтожает Power/history; suspended/archive блокирует normal effects по умолчанию.
20. Community Scope trusted explicitly; RLS является defense-in-depth, не authorization.
21. Current Placement/Generation resolved; stale Generation fenced.
22. Cross-Placement FK/SQL/ACID/2PC не предполагаются.
23. Significant multi-step processes используют specialized resumable coordinators; universal Workflow отсутствует.
24. Cross-store partial state detectable/recoverable.
25. Нет global/per-Community FIFO; ordering, exclusivity и concurrency distinct/scoped.
26. Fairness/backpressure не задают physical tenant topology или SLA.
27. Cancellation Requested, Execution Stopped и Compensation distinct; universal rollback отсутствует.
28. Secret material не входит в work/log/audit/history; privileged interventions authorized/attributed/audited.
29. Unsupported work version fail closed/quarantine без silent reinterpretation/loss.

## Последствия

Положительно:

- crash/restart/deployment не теряют значимые obligations;
- duplicate delivery и partial/unknown external outcomes получают честную семантику;
- tenant/Placement/security boundaries сохраняются в Worker runtime;
- failed work остаётся observable/resolvable;
- modules сохраняют ownership domain/integration meaning;
- runtime способен масштабировать workload classes без premature service proliferation.

Цена и риски:

- каждый effect/contract должен определить idempotency, success evidence, retry/reconciliation и authority boundary;
- требуется durable technical state и disciplined operator resolution;
- ordering/fairness/version compatibility повышают implementation/operations complexity;
- provider без idempotency/status API может оставлять manual unresolved outcomes;
- чрезмерное расширение common contract создаст central workflow anti-pattern, а слишком слабый contract — inconsistent reliability.

## Отклонённые альтернативы

- **In-memory-only significant background work.** Теряет obligations при crash/restart.
- **System-wide exactly-once promise.** Недоказуемо через DB, transport и external systems; скрывает duplicate/unknown outcomes.
- **External call внутри local transaction.** Не создаёт distributed atomicity и удерживает local resources через remote uncertainty.
- **Retry everything forever.** Создаёт poison loops, stale-authority и duplicate-effect risk.
- **Blind retry Unknown Outcome.** Может повторить material external effect.
- **Operational logs как durable work record.** Logs не authoritative state/history obligation.
- **Silent discard failed work.** Теряет business-significant obligation и traceability.
- **Universal domain Job / Workflow Engine / Event entity.** Нарушает module/context ownership ADR-002/013.
- **Mandatory Inbox everywhere.** Создаёт unnecessary central mechanism там, где authoritative uniqueness/guard sufficient.
- **Global или implicit per-Community FIFO.** Даёт ложную guarantee, head-of-line blocking и noisy-neighbor coupling.
- **Queue/Worker per Community baseline.** Преждевременно фиксирует deployment topology.
- **Captured user authorization как execution credential.** Нарушает ADR-015 revocation/Workload Identity и создаёт evergreen delegation.
- **Cancellation как rollback.** Невозможно универсально отменить committed/external effects и переписывает history.

## Отложенные решения

### ADR-017

- production Worker/broker/job-store topology и HA;
- replicas, health/readiness, monitoring/alerting и capacity;
- rollout sequencing и operational compatibility enforcement;
- quarantine/manual-resolution/recovery runbooks и drills;
- production recovery/roll-forward/rollback procedures.

### Implementation Baseline

- broker/no-broker realization, exact job/outbox/inbox storage и SQL schema;
- claim/lease/lock, polling, scheduler и cache/invalidation mechanics;
- retry/backoff/timeouts/batch sizes;
- serialization, version adapter/upcaster и migration mechanisms;
- concrete frameworks/libraries and telemetry tooling;
- exact idempotency/uniqueness implementation.

### Context/integration policy

- retry eligibility/count/timing и provider-specific reconciliation;
- success evidence, idempotency-key retention и external quotas;
- ordering key, strict-stream poison behavior и priorities;
- compensation/correction, notification expiry/delivery и import partial-success policy;
- exact human/domain authority boundary for each operation class.

## Согласованность с ADR-001–ADR-015

- **ADR-001/008:** automatic work не становится Subject; voting identities/snapshots/results/history не сводятся к job state.
- **ADR-002:** common runtime contract не является bounded context и сохраняет owning-context semantics.
- **ADR-003:** historically relevant configuration version сохраняется; universal configuration не вводится.
- **ADR-004:** Attempts/runtime logs не заменяют Domain History; retry/correction не переписывают past facts.
- **ADR-005:** использованная Rule Version determinable и не подменяется Schema/contract version.
- **ADR-006:** Payment/Intention/Allocation/correction distinct; unknown financial effect не blind-retry-ится.
- **ADR-007:** telemetry/received information/Reading/Consumption distinct; equal sample не доказывает duplicate.
- **ADR-009:** created/sent/provider-accepted/delivered/received/read/legal notification distinct.
- **ADR-010:** technical authorization и domain admissibility distinct; mandatory denial не overridden; no fake Subject.
- **ADR-011:** integration contract owns duplicate/correction/recognition/Unknown Outcome/reconciliation semantics.
- **ADR-012:** provisioning/relocation остаются persistent/resumable с stable identities, idempotent steps и explicit compensation; lifecycle respected.
- **ADR-013:** Worker/application/module contracts, durable delivery, outbox/equivalent и no universal Event/Workflow preserved.
- **ADR-014:** local transaction/persistence boundaries, no 2PC, RLS, Placement Generation и recoverable partial state preserved.
- **ADR-015:** Workload Identity, current revalidation, Secret lifecycle, privileged access и Security Audit applied without captured credentials.

## Влияние на другие документы

- `TERMINOLOGY.md` должен синхронизировать только stable runtime terms Persistent Work/Operation/Attempt, Durable Delivery/Outbox, Idempotency/selective duplicate recognition, outcome, quarantine и cancellation distinctions.
- `ARCHITECTURE_ROADMAP.md` должен показать ADR-016 как PROPOSED, Stage K ongoing, ADR-017 next-after-acceptance и implementation not started.
- `DOMAIN_MODEL.md`, `VISION.md` и `README.md` не требуют изменения: technical runtime artifacts не становятся domain model или новым product scope.
